#!/bin/sh
set -e

#DEBHELPER#

case $1 in
    remove)
        conflicts="/opt/cixgpu-compat/app.json"
        for i in $conflicts
        do
            dpkg-divert --package task-cix-gpu --rename --remove "$i"
        done
        ;;
esac

exit 0
