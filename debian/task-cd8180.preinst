#!/bin/sh
set -e

case $1 in
install | upgrade)
	conflicts="
        /usr/lib/modprobe.d/panfrost.conf
        "
	for i in $conflicts; do
		dpkg-divert --package task-cd8180 --divert "$i.bak" --rename "$i"
	done
	;;
esac

#DEBHELPER#

exit 0
