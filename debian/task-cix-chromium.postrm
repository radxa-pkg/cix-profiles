#!/bin/sh
set -e

#DEBHELPER#

case $1 in
    remove)
        conflicts="
        /usr/share/applications/gnome-mimeapps.list
        "
        for i in $conflicts
        do
            dpkg-divert --package task-cix-chromium --rename --remove "$i"
        done
        ;;
esac

exit 0
